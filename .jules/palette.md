## 2026-02-14 - Accessible Icon Buttons
**Learning:** Icon-only buttons often rely on `title` or hover states for labels, which are insufficient for screen readers and touch devices.
**Action:** Always add explicit `aria-label` to icon-only buttons and ensure `focus-visible` styles are present for keyboard navigation.
